{
  "name": "proof-of-match",
  "version": "0.1.0",
  "private": true,
  "description": "On-chain attestation layer for the proof-of-match pipeline",
  "scripts": {
    "chain": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network baseSepolia",
    "compile": "hardhat compile"
  },
  "devDependencies": {
    "hardhat": "^2.22.17"
  }
}
